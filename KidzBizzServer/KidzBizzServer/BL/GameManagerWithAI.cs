namespace KidzBizzServer.BL
{
    public class GameManagerWithAI
    {
        Player Player;
        AIPlayer AIPlayer;
        int currentPlayerIndex;
        int diceRoll;


        // פעולה להפעלת משחק חדש
        public void StartNewGame()
        {

            // לוגיקת הפעלת משחק חדש
        }

        // פעולה לסיום משחק
        public void EndGame()
        {
            // לוגיקת סיום משחק
        }

        // פעולה להפסקת משחק
        public void PauseGame()
        {
            // לוגיקת הפסקת משחק
        }

        // פעולה להמשך משחק
        public void ContinueGame()
        {
            // לוגיקת המשך משחק
        }   
    }
}
